# Port for the server (optional, defaults to 3000)
PORT=3000

# Firebase Service Account JSON (for production deployment)
# Paste the entire JSON content of your service-account.json file here (minified)
# Example: {"type":"service_account","project_id":"your-project",...}
FIREBASE_SERVICE_ACCOUNT=

# Note: For local development, you can use service-account.json file instead
# Download it from Firebase Console > Project Settings > Service Accounts
